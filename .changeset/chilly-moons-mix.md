---
"@saleor/macaw-ui": patch
---

Upgrade `eslint-plugin-react-hooks` from version 4.6.0 to 7.0.1, add react compiler ESLint rules to detect issues with incorrect hook implementation causing React to crash.
