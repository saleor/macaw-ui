---
"@saleor/macaw-ui": patch
---

Upgrade `eslint-plugin-react-hooks` from version 4.6.0 to 7.0.1. This maintains the same two rules (`rules-of-hooks` and `exhaustive-deps`) with potential bug fixes and improvements.
